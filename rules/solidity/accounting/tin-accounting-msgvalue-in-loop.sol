pragma solidity ^0.4.24;

contract Proxy {
    address owner;

    function callme(address callee, bytes _data) public {
        while(true){
            msg.value;
        }
        for(true; true; true){
            msg.value;
        }
        do {
            msg.value;
        } while (true);
        {
            msg.value;
        }
    }
}
